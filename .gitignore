# Local configuration - never commit
.env

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/

# Voice clips: the bundled voices are tracked; your own clips are not.
# Run scripts/build_voices.py to regenerate the bundled set.
voices/*
!voices/.gitkeep
!voices/*.wav

# Audio artifacts
*.wav