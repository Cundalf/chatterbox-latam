# Host port for the TTS server (SUB/WAVE Remote URL uses this)
CHATTERBOX_PORT=5005

# Voice tuning (see README for guidance)
TTS_LANGUAGE=es
TTS_EXAGGERATION=0.5
TTS_CFG_WEIGHT=0.5
TTS_TEMPERATURE=0.8

# Force the inference device: cuda | cpu (leave empty to auto-detect)
TTS_DEVICE=